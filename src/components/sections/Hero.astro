---
// src/components/sections/Hero.astro
// Above-the-fold hero. Order (per design system spec):
//   brand mark → H1 → sub → description → chips → trust pills → CTAs + risk reducer
//   → trust line → training photo
// Right column: editorial training photo (Reese pointing at Canva screen).

export interface Props {
  locale: 'en' | 'zh';
}
const { locale } = Astro.props;
const isZh = locale === 'zh';
---

<section class="section section--hero" id="hero">
  <div class="container-2xl">
    <div style="display: grid; grid-template-columns: 1fr; gap: 48px;" class="hero-grid">
      <style>
        @media (min-width: 1024px) {
          .hero-grid { grid-template-columns: 1.1fr 0.9fr !important; align-items: center; gap: 64px !important; }
        }
      </style>

      <!-- LEFT: copy + CTAs + booking -->
      <div>
        <!-- Brand mark: Reese + planet logo -->
        <div style="display: flex; align-items: center; gap: 2px; margin-bottom: 24px;">
          <img src="/images/reese-logo-light.png" alt="Reese" style="display: block; height: 56px; width: auto;" width="200" height="70" />
        </div>

        <!-- H1 — kinetic Fraunces (Option B signature) -->
        <h1 class="kinetic-fraunces" style="margin: 0 0 16px;">
          {isZh ? '中小企业与企业的 AI 培训师' : 'AI Trainer for SMEs & Corporate Teams'}
        </h1>

        <!-- Sub-headline -->
        <p style="font-size: 1.25rem; line-height: 1.4; color: var(--foreground); max-width: 50ch; margin: 0 0 24px;">
          {isZh ? '每周别再浪费 10 小时在手动工作上。' : 'Stop wasting 10 hours a week on manual work.'}
        </p>

        <!-- Trust pills -->
        <div style="display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 32px;">
          <span class="badge badge--outlined" style="font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; font-size: 11px;">{isZh ? 'HRDC 认证' : 'HRDC ACCREDITED'}</span>
          <span class="badge badge--outlined" style="font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; font-size: 11px;">{isZh ? 'HRD CORP 可申请' : 'HRD CORP CLAIMABLE'}</span>
          <span class="badge badge--outlined" style="font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; font-size: 11px;">{isZh ? 'LPPEH 5 CPD 学时' : 'LPPEH 5 CPD HOURS'}</span>
        </div>

        <!-- Description -->
        <p style="color: var(--muted-foreground); max-width: 55ch; margin: 0 0 24px;">
          {isZh
            ? '一次 5 小时的实战培训,团队学会用免费 AI 工具把内容创作时间减少 70%、写作更快、研究更智能,并搭建出每天都在用的 AI 工作流。'
            : 'In one hands-on session, your team learns to cut content creation time by 70%, write faster, research smarter, and build AI workflows they\'ll use every single day — using only free tools.'}
        </p>

        <!-- Chips -->
        <div style="display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 32px;">
          <span class="badge badge--subtle">⏱ {isZh ? '每周节省 10+ 小时' : 'Save 10+ hrs/week'}</span>
          <span class="badge badge--subtle">📣 {isZh ? '写作快 10 倍' : 'Write 10x faster'}</span>
          <span class="badge badge--subtle">📊 {isZh ? '秒级研究' : 'Research in seconds'}</span>
          <span class="badge badge--subtle">🚀 {isZh ? '团队效率倍增' : 'Boost team productivity'}</span>
        </div>

        <!-- CTAs + risk reducer -->
        <div class="hero-cta" style="margin-bottom: 40px;">
          <div style="display: flex; flex-wrap: wrap; gap: 12px;">
            <a href="#booking" class="btn btn--primary btn--lg">
              {isZh ? '预约免费咨询' : 'Book a Free Consultation'}
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </a>
            <a href="#training" class="btn btn--secondary btn--lg">
              {isZh ? '了解课程' : 'Explore Training'}
            </a>
          </div>
          <div class="hero-cta__reducer">
            {isZh ? '免费 15 分钟通话 · 无承诺 · 周一至周五 9–6 (MYT)' : 'Free 15-min call · No commitment · Mon–Fri 9–6 MYT'}
          </div>
        </div>

        <!-- Trust line -->
        <div style="display: flex; align-items: center; gap: 12px; padding: 16px 0; border-top: 1px solid var(--border);">
          <div style="display: flex; gap: 2px;">
            {[1,2,3,4,5].map(() => (
              <svg width="16" height="16" viewBox="0 0 24 24" fill="oklch(75% 0.18 60)">
                <path d="M12 .587l3.668 7.568L24 9.75l-6 5.847L19.336 24 12 19.897 4.664 24 6 15.597 0 9.75l8.332-1.595z"/>
              </svg>
            ))}
          </div>
          <div style="font-size: 14px;">
            <strong>4.8/5</strong> <span style="color: var(--muted-foreground);">{isZh ? '来自 Google 评论' : 'from Google Reviews'}</span>
          </div>
          <div style="font-size: 14px; color: var(--muted-foreground); margin-left: auto;">
            {isZh ? '已培训 500+ 专业人士' : '500+ professionals trained'}
          </div>
        </div>
      </div>

      <!-- RIGHT: in-the-room coaching photo (Reese + participant, Hartamas® room) -->
      <div style="position: relative;">
        <img
          src="/images/training-coaching.jpg"
          alt={isZh ? 'Reese Tan 现场指导学员' : 'Reese Tan coaching a participant'}
          style="width: 100%; aspect-ratio: 4/3; object-fit: cover; object-position: center 15%; border-radius: var(--radius-lg);"
          loading="eager"
          fetchpriority="high"
          width="2048"
          height="1536"
        />
      </div>
    </div>
  </div>
</section>
